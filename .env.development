# Override production settings for local development
NEXTAUTH_URL=http://localhost:3000

# For local development, you can use either the Firebase domain or localhost
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=localhost:3000
# Keep using the Firebase domain for development to avoid configuration issues
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=hyper-flix-f2891.firebaseapp.com 