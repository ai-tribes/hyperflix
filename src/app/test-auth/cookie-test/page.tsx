import CookieTest from '../cookie-test';

export const metadata = {
  title: 'Authentication Diagnostics | HyperFlix',
  description: 'Test and diagnose authentication, cookie, and CSP issues',
};

export default function CookieTestPage() {
  return <CookieTest />;
} 